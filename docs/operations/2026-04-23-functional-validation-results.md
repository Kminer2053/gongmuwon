# 기능 검증 결과 - 2026-04-23

## 범위

- 검증 환경: Windows 메인 Codex 개발 환경
- 검증 기준 문서: [2026-04-23-functional-validation-checklist.md](C:/Users/USER/Agent_Gongmu/Agent_Gongmu_Codex/docs/operations/2026-04-23-functional-validation-checklist.md)
- 검증 방식: 사용자 수동 점검 + 개발 배치 수정 + 회귀 검증

## 현재 요약

- 핵심 기능 흐름은 현재 기준으로 대부분 닫혔다.
- 최초 점검에서 막혔던 blocker 3개는 모두 수정됐다.
  - 문서작성 `Content Base 생성 실패`
  - 내 지식폴더 `승인 실패`
  - 파일정리 `승인 후 적용 409`
- Anything 연계는 `내장 배포`가 아니라 `외부 설치형 연계` 계약으로 정리됐다.
- 설치 패키지 점검은 지금 단계의 우선순위가 아니며, 기능 완성도 확인을 먼저 진행한다.
- 2026-04-25 기준 후속 배치로 `상단 기능 탭 + 좌측 세션 레일 + 접이식 우측 패널` 구조와 캘린더 내부 편집 패널이 반영됐다.

## 배치별 결과

## A. 핵심 업무 흐름

### A1. 일정 생성

- 상태: `pass`
- 사용자 확인:
  - 일정 생성
  - 일정 카드/목록 반영
  - 우측 선택 상태 반영
- 후속 반영:
  - 일정 화면 기준선을 `day / week / month` 보기로 맞춤
- 남은 의견:
  - 현재 목록형 UI보다 `일/주/월 캘린더` 기반 일정 생성 UX가 더 적합하다는 피드백이 있었다.

### A2. 업무 세션 연결

- 상태: `pass`
- 사용자 확인:
  - 선택 일정과 연결된 세션 생성/열기
  - 우측 선택 세션 상태 반영
- 후속 반영:
  - 업무 세션 목록을 좌측 `세션 레일`로 재배치
  - 중앙은 `세션 캔버스` 중심으로 재정리
  - 우측 `현재 컨텍스트 / 승인 요청 / 최근 실행`은 접기/펼치기 가능한 보조 패널로 변경
- 남은 의견:
  - 제품의 주 진입은 `일정`보다 `업무대화`가 되어야 한다.
  - 업무대화는 일반적인 LLM 채팅처럼 세션 목록/저장 구조가 더 선명해야 한다.
  - 권장 흐름:
    - 경로 1: 업무대화 중 일정 연결
    - 경로 2: 일정에서 해당 시점 세션 열기

### A3. 수동 Reference Set 생성

- 상태: `pass`
- 사용자 확인:
  - Reference Set 생성
  - 선택 상태 반영
- 남은 의견:
  - `Reference Set`의 역할 설명이 부족하다.
  - 검색 결과를 드래그하거나 바로 묶는 UX가 있으면 더 자연스럽다.

### A4. Content Base 생성

- 최초 상태: `fail`
- 현재 상태: `pass`
- 수정 내용:
  - 문서작성 fetch 실패 경로 수정
  - Content Base 생성/미리보기 회귀 검증 통과

### A5. 초안 변경 후 stale 보호

- 최초 상태: `fail`
- 현재 상태: `pass`
- 수정 내용:
  - Content Base 생성 이후 제목/목적/템플릿/Reference Set이 바뀌면 기존 초안을 재사용하지 못하도록 보호

### A6. 최종 문서 승인 및 적용

- 최초 상태: `fail`
- 현재 상태: `pass`
- 수정 내용:
  - 최종 저장 요청 -> 승인 -> 적용 -> 산출물 경로 표시 흐름 재검증 완료

## B. Anything / 검색 / 문서작성 연결

### B1. Anything 실행 요청

- 최초 상태: `partial`
- 현재 상태: `pass`
- 수정 내용:
  - `Anything 외부 설치형 연계` 계약으로 정리
  - 미설치 시 설치 안내, 설치 시 외부 실행으로 분기

### B2. Anything 실행 및 다시 열기

- 최초 상태: `fail`
- 현재 상태: `pass`
- 수정 내용:
  - Windows 로컬 설치본 감지
  - 승인 후 외부 앱 열기 / 다시 열기 검증 경로 확보
- 참고:
  - 감지된 실행 파일 경로: `C:\Users\USER\AppData\Local\Anything\docufinder.exe`
  - `GONGMU_ANYTHING_AUTOPASTE=1`일 때는 Windows에서 실험적 자동 붙여넣기를 시도
  - 기본값은 안전한 클립보드 handoff

### B3. Anything 결과를 Reference Set으로 가져오기

- 최초 상태: `fail`
- 현재 상태: `pass`
- 수정 내용:
  - 경로 붙여넣기 -> 새 Reference Set 가져오기 -> 문서작성 재료로 사용 가능

### B4. Continue to Documents handoff

- 최초 상태: `미평가`
- 현재 상태: `pass`
- 수정 내용:
  - `문서작성으로 이어가기` 버튼 정리
  - 제목/목적 기본값 handoff
  - 선택된 Reference Set 요약 카드, 대표 파일명, 대표 경로 표시

## C. 내 지식폴더

### C1. 지식 후보 생성

- 상태: `pass`
- 사용자 확인:
  - 후보 생성
  - pending 목록 반영
- 남은 의견:
  - 후보 카드를 눌러 상세 내용을 볼 수 있어야 한다.

### C2. 지식 페이지 승인

- 최초 상태: `fail`
- 현재 상태: `pass`
- 수정 내용:
  - 승인 fetch 실패 수정
  - 이미 승인된 후보 재승인 방지

### C3. 지식 검색

- 최초 상태: `fail`
- 현재 상태: `pass`
- 수정 내용:
  - 지식 페이지 반영 이후 검색/graph 요약 흐름 재검증 가능 상태

## D. 파일정리 / 승인 / 롤백

### D1. 제안 생성

- 상태: `pass`
- 사용자 확인:
  - 제안 생성
  - 제안 목록 표시
- 남은 의견:
  - 대상 경로를 파일 탐색기로 선택하는 UX가 더 적합하다.
  - 제안 카드 상세보기 필요

### D2. 승인 후 적용

- 최초 상태: `partial`
- 현재 상태: `pass`
- 수정 내용:
  - 폴더 proposal apply 경로 수정
  - 승인 후 적용 409 문제 수정

### D3. 롤백

- 최초 상태: `fail`
- 현재 상태: `pass`
- 수정 내용:
  - 적용 -> 롤백 -> 재요청 흐름 복구
  - UI 상태 재계산 및 회귀 테스트 통과

### 파일정리 기능 해석

- 현재 구현은 `완성형 자동 이동 정리기`라기보다 `승인형 정리 작업 레지스트리`에 가깝다.
- 이미 실제 파일시스템 반영과 롤백은 수행한다.
- 다만 사용자 관점에서는 다음 보강이 필요하다.
  - 목적 설명 강화
  - 제안 상세보기
  - 경로 선택 UX
  - 더 명확한 분류/정리 목적 표현

## E. 런타임 / 승인 / 실행기록

### E1. managed sidecar 시작 / 재시작 / 종료

- 상태: `pass`

### E2. unmanaged sidecar 복구

- 상태: `pass`
- 사용자 확인:
  - 강제 종료 후 자동 복구 동작 확인

### E3. 승인 큐 일관성

- 상태: `pass`

### E4. 실행기록

- 상태: `partial`
- 사용자 의견:
  - 기록 자체는 남지만 로그만 봐서는 이해가 어렵다.
  - 한글화/설명 강화와 카드 상세보기 UX가 필요하다.

## 남은 주요 UX/IA 개선 과제

현재 blocker는 해소됐지만, 다음 개선 배치에서 우선적으로 다룰 가치가 높은 항목은 아래와 같다.

1. 자유형 업무대화 자체의 메시지/스레드 모델 확장
2. 일정 칸 내부 표시 밀도와 직접 수정 UX 보강
3. Reference Set의 역할을 더 분명한 제품 언어로 정리
4. 지식 후보 / 파일정리 제안 / 실행기록의 상세보기 패턴 공통화
5. 파일정리 경로 선택 UX 개선

## 최신 검증 기준선

2026-04-25 기준 최신 개발 검증:

- `npm.cmd run sidecar:test` -> PASS (`28 passed`)
- `npm.cmd run desktop:test` -> PASS (`32 passed`)
- `npm.cmd run desktop:build` -> PASS
- `node scripts/portable-run.mjs cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` -> PASS
- `npm.cmd run verify:all` -> PASS
- `npm.cmd run desktop:smoke:nsis` -> PASS

## 결론

- 기능 우선 검증 관점에서 `현재 치명적 blocker는 없다`.
- 다음 단계는 설치 패키지보다 `업무대화 중심 IA`, `캘린더형 일정 UX`, `카드 상세보기/기록 가독성` 같은 제품 완성도 개선 배치가 더 맞다.
