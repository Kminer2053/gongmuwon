# 최종산출물 품질 리뷰 백로그 (사용자 리뷰 세션)

- 시작: 2026-07-04 · 방식: 사용자 지적 + AI 발견 병행, 코드 원인 정밀 확인 후 개선방향만 기록 (수정은 세션 종료 후 일괄 결정)
- 표기: F-xx = 리뷰 중 확정 항목, M-xx = AI가 개발/검증 과정에서 발견해 둔 잔여 항목

## 확정 항목 (F)

| # | 우선순위 | 항목 | 코드 원인 | 개선 방향 |
| --- | --- | --- | --- | --- |
| F-01 | P1 | 응답 본문에 원본 절대경로가 통째로 삽입되어 가독성 파괴 | `app.py:1012` 프롬프트가 "제목·원본 경로" 인용을 지시 + 근거 블록에 절대경로 포함(1027) → 모델이 경로를 본문에 복사 | 프롬프트는 "(출처: 제목)"만 지시. 경로는 응답 카드 하단 출처 칩(열기/복사 액션)으로 구조화 — context_summary의 구조 데이터 활용 |
| F-02 | P1 | 실패 턴 표기 3중 문제: 성공 같은 `응답 2.2초` 배지, 영어 원시 예외 노출, 다음 행동 안내 없음 | `app.py:1800` `f"...실패했습니다.\n\n{exc}"` 원시 예외 연결 · `app.tsx:4738` 배지가 status 무관 렌더 | 실패 시 적색 `실패` 배지, 예외는 한국어 요약 + 접힌 상세, [환경설정으로 이동] 액션 버튼 |
| F-03 | P2 | 컴포저 플레이스홀더에 내부 용어("코덱스처럼...") | `app.tsx:4955` 하드코딩 | 기능 유도형 카피로 교체 (예: "업무 요청이나 메모를 입력하세요. 예: 지식폴더에서 ○○ 근거 찾아줘") |
| F-04 | P1 | 클립보드 이미지 붙여넣기 미지원 | 컴포저에 onPaste/clipboardData 핸들러 0건 — 첨부는 file input(4963)만 | textarea onPaste에서 image/* 추출 → 기존 첨부 업로드 흐름 재사용(파일명 자동 부여). 드래그&드롭 동일 경로 |
| F-05 | **P0** | 응답 가독성 붕괴 — 줄바꿈/블릿/들여쓰기 소실로 벽 텍스트 | 수제 렌더러 `app.tsx:1028-1220`: ① `* ` 별표 블릿 미인식(`- `/`1.`만) ② 문단이 연속 줄을 공백 병합(1201) ③ 전 줄 trim으로 중첩 들여쓰기 소실 ④ 180자 문장 재분할이 원문 왜곡 | 블릿 문자 정규화(`*`/`•`/`◦`→`- `), trim 전 깊이 계산으로 중첩 ul, 단일 개행 보존, 문장 재분할 제거(무개행 원문 한정). 대안: 경량 마크다운 파서 도입 + 커스텀(테이블·열기버튼) 이식 |

### 문서작성 화면 리뷰 (2026-07-04 스크린샷 2장)

| # | 우선순위 | 항목 | 코드 원인 | 개선 방향 |
| --- | --- | --- | --- | --- |
| F-06 | P1 | HWPX 미리보기가 평문뿐 — ax-playground처럼 rhwp 종이 미리보기 필요 | 미리보기 탭은 서버 preview 텍스트 `<pre>`만 렌더 | **→ D-02 결정** (rhwp WASM 내장) |
| F-07 | P1 | 참고자료 탭 가독성 — 연결 파일이 "202506 202507 … 합계 · 경로" 원시 발췌로 표시 | 링크 label이 `knowledge_source_files.title`(=본문 첫 줄, 표 PDF는 헤더행이 제목이 됨)로 저장됨(`connectLocalFileToSession`이 `hit.file.title` 사용) + 참고자료 탭이 label·경로만 나열(`app.tsx:6142-6147`) | ① 링크 생성 시 위키 문서 카드 제목 우선(같은 문서의 카드에는 정상 제목 존재), 없으면 파일명 ② 참고자료를 카드형으로: 유형 아이콘 + 제목 + 경로(보조) + 위키 카드 개요/키워드 발췌, 펼쳐보기 |
| F-08 | P1 | 작성 컨텐츠 수정을 수동 편집만 지원 — LLM 수정 지시 채팅 필요 | 위계 편집기/JSON 토글만 존재, revise 경로 없음 | 사이드카에 `POST /api/documents/authoring/revise` (현재 구조+수정 지시 → 스키마 검증된 새 구조, format 단계 재사용) + 작성 컨텐츠 탭에 "수정 지시" 입력창(예: "예산 섹션을 앞으로, 요약은 2문장으로") → 편집기 갱신 + 변경 요약 표시. 반복 지시 가능 |
| F-09 | P1 | [이대로 문서 생성] 버튼이 최종 HWPX 탭에 있음 — 미리보기를 보고 결정하는 흐름과 어긋남 | 빌드 버튼이 final 탭(`app.tsx:6266`), 미리보기 탭에는 "생성 후 갱신됩니다" 안내(`6251`)만 | 탭 의미 재정렬: 작성 컨텐츠(편집·수정지시) → **미리보기(확인 + [이대로 문서 생성])** → 최종 HWPX(산출물·승인·다운로드 전용). M-09(클라이언트 즉시 미리보기)와 묶어 해결 |
| F-10 | P2 | "고급: 사용자 HWPX/HWTX 양식" = ax-playground 임의서식 기능으로 대체 검토 | 현재는 양식 파일 업로드→기존 generate 흐름 연결 수준 | ax 패턴 이식 검토: kordoc `fill --dry-run`으로 폼 필드 자동 감지(LLM 무관) → 필드 ≥3개면 "값 찾기" 프롬프트+검토 UI(근거 없으면 비움) → kordoc `fill`; 문서형은 classify(keep/replace)→재작성→`patch`(표·서식 보존). D-01(kordoc 내장)과 시너지 |
| F-11 | P2 | "아직 생성된 Content Base가 없습니다" — 내부 용어 노출 + 불필요한 빈 상태 안내 | `app.tsx:6350-6351` 하드코딩 | F-09 재배치 후 최종 탭 빈 상태는 "생성된 문서가 여기에 표시됩니다" 수준으로 축소하거나 제거. 사용자 노출 문구에서 "Content Base" 용어 제거(내부 개념) |

### 내 지식폴더 화면 리뷰 (2026-07-04 스크린샷 3장)

| # | 우선순위 | 항목 | 코드 원인 | 개선 방향 |
| --- | --- | --- | --- | --- |
| F-12 | P1 | 대시보드가 읽기 전용 표시판 — 컨트롤 패널이어야 함. 색인 상태 정보 빈약, 바로가기 버튼이 상단 탭과 중복 | 상태 카드 3종이 표시 전용, 바로가기 3버튼이 탭바 바로 아래 중복 | 카드를 액션형으로: 검색 엔진 카드=[재색인] 액션, 파서 카드=상태 상세/문제 시 안내, LLM 보강 카드=**켜기/끄기 토글 + [보강 시작]**. 색인 상태 카드에 최근 작업 이력(일시·신규/변경 건수·실패 목록 링크) 확충. 바로가기 3버튼 제거 |
| F-13 | P1 | 색인 처리 탭 레이아웃 어수선 — 설정·운영·진단이 뒤섞임. **위키 구성 기준(분류체계) 설정 기능 부재** | 상세 데이터 아코디언(소스 설정/메타데이터/반영 기록)이 최상단, 그 아래 흐름 안내·카운트·보강 버튼·폴더 작업 순 — 우선순위 역전 | 재배열: ① 등록 폴더 + 스캔/색인/재색인 액션(최상단) ② 진행 상태(작업 카드) ③ LLM 보강 ④ 진단/상세 데이터(접힘, 최하단). **위키 구성 기준 설정은 T-01 분류체계와 함께 설계** |
| F-14 | P1 | 검색탭 UX 2건: ① Enter로 실행 안 됨(버튼 클릭 강제) ② 새 검색 시 이전 결과/근거답변이 화면에 잔존해 누적처럼 보임 — 히스토리 개념 부재 | ① 입력이 form 밖, onKeyDown 없음(`app.tsx:7075`) ② `knowledgeSearchResult`/`knowledgeAskResult`가 독립 상태로 서로 리셋하지 않고 스택 렌더 | **단일 질의 모델**로 재설계: 검색 방법 선택(키워드 검색 | 근거 답변) + Enter 실행, 결과 영역은 항상 최신 질의 1건만 표시. 과거 질의는 히스토리 칩/목록(클릭 시 재실행)으로 분리. 검색↔근거답변 전환 시 상호 리셋 |
| F-15 | **P0(설계)** | 위키가 폴더 내 전 문서를 개별 지식화 — 체계 없음. Work-Aware 목표(업무·역할 파악 → 문서 종류 구분 → 체계화) 미달 | 현 구조: docs/ 평면 카드 + topics(LLM 보강 시) + work/ — 업무 축 분류 부재 | **→ T-01 심층 과제로 설정** (최초 색인 시 대화형 분류체계 수립 → 색인·위키가 체계를 따름). 사용자와 심층 대화로 로직 확정 후 구현 |

### 위키·일정·컨텍스트 리뷰 (2026-07-04, 일정은 AI가 E2E 직접 점검)

| # | 우선순위 | 항목 | 코드 원인 / 실측 | 개선 방향 |
| --- | --- | --- | --- | --- |
| F-18 | P1 | 위키 페이지 뷰어 가독성 — 정보가 "이쁘게" 표출되도록 디자인 요소 필요 (사용자 지시) | 위키 페이지가 마크다운 원문 렌더 그대로 (front matter 흔적, 밋밋한 목록) | 문서 카드 페이지를 **구조화 레이아웃**으로: 상단 메타 헤더(유형·일자·품질·원본 열기 버튼), 개요 하이라이트 박스, 섹션 아웃라인은 접이식, 키워드 칩, 관련 문서/업무기록은 카드 그리드. 타이포 위계(제목/본문/메타) 강화. index/topics/work 페이지별 전용 템플릿 |
| F-19 | **P0** | 일정 주간 뷰 가독성 (E2E 실측): ① 시간축 없음 — 요일별 텍스트 카드 나열 ② **100% 배율에서도 이벤트 카드 전 줄이 잘림**(`schedule-slot__line` overflow 실측) ③ 카드 정보 과잉 — 제목 3회 반복 + 정체불명 "1" 배지 + "클릭해 일정 편집" 안내 매 카드 반복 ④ 요일 헤더 이중 표기(그리드 헤더 + 셀 내) ⑤ 하단 폼 "보기" 셀렉트가 상단 월/주/일 토글과 중복 (5월 감사 잔존) | 주간 뷰가 시간 그리드가 아닌 day-card 리스트 구조. 7열 좁은 칸에 긴 문장 주입 | 2026-05-26 시안의 **시간축 그리드**(top=시작시각, 높이=길이, now line) 적용 + 이벤트 블록은 "시각+제목"만, 상세는 클릭/호버. 요일 헤더 단일화, 하단 보기 셀렉트 제거. ※확대 내성은 통과(100~150% 가로 스크롤 없음 실측) |
| F-20 | P1 | **일정 알림 기능 미구현** (설정·운영 체크 결과: 존재하지 않음) | Schedule 모델 필드가 title/starts_at/ends_at/view뿐 — 알림 필드·스케줄러·OS 알림 연동 전무. PRD "회의 30분 전 준비 알림" 목표 미달 | ① 모델에 remind_before(분) 추가 ② 사이드카 스케줄러(기존 job_runner 재사용)가 도래 일정 감지 ③ Tauri notification 플러그인으로 OS 알림 + 앱 내 토스트 ④ 알림 클릭 → 해당 일정/연결 세션 열기. 기본값 정책(회의류 30분 전 등)은 T-01 논의와 연계 가능 |

## 설계 과제 (T)

| # | 과제 | 현황 실측 (2026-07-04 코드 추적) | 설계 방향 초안 |
| --- | --- | --- | --- |
| T-01 | Work-Aware 지식분류체계 (심층 대화 — **최후 순서로 연기**) | 위키가 폴더 내 전 문서를 평면 개별 지식화 | 3축(업무×유형×시간) + 최초 색인 시 분류 인터뷰 + SCHEMA.md — 상세는 본 문서 상단 F-15 및 대화록 참조 |
| T-02 | **컨텍스트 예산 관리 (경량 로컬모델 대응)** — 사용자 위임 과제 | ① 채팅: 세션 **전체 대화 이력을 무제한 주입**(`app.py:1750` 루프 — 최대 리스크, 장기 세션에서 경량모델 컨텍스트 초과) ② 문서작성: transcript **전체 무제한**(`document_authoring.py:1206`) ③ 첨부: 앞 4,000자 고정 절단(뒷부분 소실, 반영 범위 비표시) ④ 지식 근거: 5건×500자(적정) ⑤ 연결 파일: 본문 직접 주입 없음(검색 부스트만 — 토큰 안전) ⑥ reference_texts: 6,000자 캡 | **블록별 예산 배분기**: 모델 컨텍스트 한도 기준 가드레일→지식근거→첨부발췌→최근 N턴 원문→이전 구간 누적요약 순 할당. **대화 이력**: 최근 N턴 + 증분 세션 요약(기존 세션 분석 재사용, work/ 페이지와 동원화). **첨부/연결파일**: 통짜 절단 대신 업로드 시 임시 FTS 색인(위키 인프라 재사용) → 턴 질의 연동 top-k 발췌 + 카드식 개요, ax-playground식 "N자 중 M자 반영" 투명 배지. **문서작성**: organize 전 대화 압축 전처리(최근 지시 원문+이전 요약). 측정: 입력 토큰 추정치를 context_summary/실행기록에 기록 |

## 확정 결정 (D) — 추가

| # | 결정 | 근거 | 구현 스케치 |
| --- | --- | --- | --- |
| D-02 | **rhwp WASM 미리보기 내장** (사용자 확정, 2026-07-04) | @rhwp/core **MIT 확인**(v0.7.17) — 재배포 가능. ax-playground에서 실증된 패턴 | ① `@rhwp/core` 의존성 추가(WASM ~6MB, 로컬 번들 — 폐쇄망 OK) ② 동적 import + 탭 진입 시 선로딩 ③ hwpx 바이트 → 페이지별 SVG, 종이 스타일(흰 배경·그림자) + `‹ N/총 ›` 페이지네이션 ④ "근사 미리보기 — 실제 한컴 서식과 다를 수 있습니다" 상시 고지 + 근사/텍스트 이중 뷰 유지 ⑤ 빌드 산출 hwpx를 바로 렌더(F-09의 미리보기 탭에서) |
| D-03 | **폴링 구조 개선안 채택** (사용자 확정, 2026-07-04) | M-01 분석 결과 승인 — "제안한 방향대로" | ① 활성 잡 감시는 `GET /api/jobs` 1종만 1.5초 ② 셸 나머지(설정·일정·세션·승인 등)는 유휴 30초 주기 또는 SSE 이벤트 푸시(턴 스트림 인프라 재사용)로 전환 ③ settings/templates는 주기 폴링에서 제외(변경 액션 후에만 재조회) ④ 서버측 stale queued 잡 TTL 자동 정리(예: 30분 경과 시 canceled 처리 + 실행기록 남김) ⑤ isActiveWorkJob의 queued 포함은 유지하되 TTL이 재발 방지 |
| D-04 | **파일찾기·파일정리 기능 삭제 + 세션 파일 연결은 업무대화 모달로 이전** (사용자 확정, 2026-07-04) | 지식폴더 검색(본문 FTS5)과 위키가 탐색 니즈를 흡수, 파일정리는 실사용 부재 | ① 화면 2종 + 나브 항목 삭제 ② 채팅 툴바 [파일 연결] 버튼 → 검색+연결 **모달**(로컬 파일명 인덱스 검색 재사용, 결과 행 [연결]) ③ Anything 연계 완전 삭제(런처·설치 안내·승인 흐름 포함) ④ api.ts/sidecar의 file-organizer·anything 엔드포인트 제거, local-file-search는 모달용으로 유지(DB 테이블 비파괴) ⑤ 문서작성 참고자료는 세션 연결 파일 표시 유지 |
| D-05 | **메뉴 6개 체계 확정: 업무대화 · 일정 · 문서작성 · 내 지식폴더 · 실행기록 · 환경설정** (사용자 확정, 2026-07-04 — 실행기록 환경설정 이동안은 철회) | 삭제 2종 반영 후 실행기록은 상단 유지 | 나브 8→6. 실행기록은 별도 가독성 재설계(F-17) |

| D-06 | **홈(오늘의 브리핑) 화면 신설** (사용자 확정, 2026-07-04 — "유용성을 인식하고 자꾸 활용하게") | 일정·지식폴더 기반 오늘의 작업 제안 + 기능 활용 후크로 리텐션 확보 | 진입: 앱 시작 시 첫 화면 + **상단 타이틀 영역 클릭 = 홈 복귀**(타이틀의 홈 버튼화, 나브 6개 유지). 카드 5종: ① **오늘 일정** — 오늘/임박 일정 + 연결 세션 열기 + [회의 준비 시작](일정 제목으로 지식 검색 프리필) ② **이어서 하기** — 미완 세션·승인 대기 문서·작성 중 구조 ③ **지식 브리핑** — 최근 색인 N건·미보강 알림+[보강 시작]·새 업무 기록 ④ **오늘의 제안** — 일정 제목×지식폴더 FTS 결정론 매칭("내일 ○○ 일정 — 관련 문서 5건으로 보고 초안 시작?") → 문서작성 프리필. LLM 합성은 옵션(1일 1회 캐시) ⑤ **기능 후크** — 실행기록 기반 미사용 기능 안내(예: 이미지 붙여넣기, 세션 지식 반영), 닫기 가능. **후크 원칙**: 카드 최대 6장·카드당 단일 CTA·닫으면 당일 스누즈·매일 갱신. 데이터는 전부 기존 API(일정/세션/잡/승인/위키 트리/실행기록) — 신규 LLM 의존 없음 |

### 실행기록 리뷰 (2026-07-04)

| # | 우선순위 | 항목 | 코드 원인 | 개선 방향 |
| --- | --- | --- | --- | --- |
| F-17 | P1 | 실행기록이 "너무 시스템 로그틱" — 카드마다 보일러플레이트 반복, 도움이 되는 정보 구조 아님 | 실행기록 카드가 제목+정형 문구 2줄+타임스탬프+상세보기 반복(1건당 세로 공간 과다), 필터·그룹 없음 | **행 리스트 재설계**: 시간(오늘/어제 그룹) · 무엇을(한국어 요약 + 대상명) · 결과 배지, 1행 클릭 시 상세 펼침. 상단 카테고리 필터 칩(일정/대화/지식/문서/설정/실패) + 기간. 실패·경고는 행 배경 강조. "N건 처리·M초" 같은 유용 메타를 요약에 승격, raw JSON은 개발자 정보로 접힘 |

### 환경설정 리뷰 (2026-07-04)

| # | 우선순위 | 항목 | 코드 원인 | 개선 방향 |
| --- | --- | --- | --- | --- |
| F-16 | P1 | 환경설정이 나열식 — 섹션 구분 약하고, **저장된 프로필 재사용이 어려움**(모드/preset을 다시 골라야 값이 로드되는 구조) | 단일 폼에 LLM 정책→preset→키/URL→개인화→저장 버튼이 세로 나열. 저장 프로필은 요약 카드 표시만 있고 원클릭 전환 없음 | **프로필 카드 중심 재설계**: ① "모델 연결" 섹션 = 저장된 프로필 카드 목록(공급자·모델·상태 표시) + 카드의 [이 프로필 사용] 원클릭 활성화 + [수정]/[새 프로필] ② "개인화" 섹션 ③ "시스템 정보"(경로, 읽기 전용, 접힘) ④ "실행기록" 하부 탭(D-05). 2026-05-18 감사 권고("프로필 카드 요약")와 일치 |

## AI 발견 잔여 항목 (M) — 개발·검증 중 발견, 미수정 상태

| # | 우선순위 | 항목 | 근거/원인 | 개선 방향 |
| --- | --- | --- | --- | --- |
| M-01 | P1 | 셸 폴링 과다: 활성 잡 존재 시 API 9종을 1.5초마다(초당 6건) 재조회. **진범 확인**: `queued` 상태로 영구 잔존하는 고아 색인 잡 1건 때문에 유휴 상태에서도 최고 빈도 폴링에 갇혀 있었음 (해당 잡은 리뷰 중 취소 완료) | `app.tsx:2067` 셸 폴링(1500ms, 9종) + `app.tsx:640` isActiveWorkJob이 queued 포함 + 서버에 stale queued 잡 TTL 없음 | **→ D-03으로 결정됨 (아래)** |
| M-02 | P2 | index.md가 재색인 전까지 옛 구조(주제/업무기록 섹션 없음·중복 라인 잔존) | dedupe·섹션 개편이 색인 시점에만 반영 — 기존 파일은 그대로 | "index 재생성" 경량 액션(재색인 없이 목차만 rebuild) 또는 lint가 자동 수리 |
| M-03 | P2 | LLM 보강 미실행 상태에서 위키 '주제' 섹션이 빈 채로 노출 | enrich 배치(421건 × LLM) 미실행 — topics 0 | 위키 트리 빈 주제 상태에 안내+[LLM 요약 보강 시작] 인라인 유도. 보강을 신규 문서만 증분 실행하는 옵션 |
| M-04 | P2 | 근거 답변/채팅 지연 편차 큼(13~33초, 외부 API 의존) — 대기 중 피드백 부족 | featherless 응답 시간 변동. ask는 단일 호출로 중간 상태 없음 | ask에도 단계 표시(검색 완료 ✓ → 합성 중 …) 또는 스트리밍. 외부모델 Q2 기준 45초로 조정 |
| M-05 | P2 | work 페이지 인용 문서 감지가 best-effort 문자열 매칭 | [지식폴더 근거] 블록이 turn에 구조적으로 저장되지 않아 메시지 본문 대조로 추정 | turn 저장 시 근거 doc_id 목록을 메시지 메타로 기록 → 정확한 인용 추적 |
| M-06 | P1(릴리스) | kordoc(node) 폐쇄망 번들 파이프라인 미반영 — 미포함 시 .hwp 170종이 메타데이터 스텁으로 저하 | package.json에는 추가됐으나 release/offline 스크립트에 node_modules/kordoc 동봉 없음 | **→ D-01로 결정됨 (아래)** |

## 확정 결정 (D)

| # | 결정 | 근거 | 구현 스케치 |
| --- | --- | --- | --- |
| D-01 | **kordoc을 설치파일에 내장한다** (사용자 확정, 2026-07-04) | 라이선스 MIT 확인(github.com/chrisryugj/kordoc) — 재배포 허용. .hwp가 실사용 폴더의 최다 포맷(170/533) | ① NSIS 리소스에 node_modules/kordoc(+순수 JS 의존성) 동봉 ② kordoc은 Node 런타임 필요 → **포터블 node.exe(~60MB) 동봉 + `GONGMU_NODE_EXE`로 경로 지정**(코드가 이미 이 env 지원, kordoc_bridge.py) — 대안: Node SEA 단일 exe 컴파일 ③ THIRD-PARTY-NOTICES에 kordoc(MIT)·Node.js 고지 추가 ④ desktop:bundle 스크립트에 복사 단계 + 설치 후 kordoc_status 스모크 확인 |
| M-07 | P2(릴리스) | 레거시 GraphRAG 모듈 파일 잔존(미배선: graphrag_ingestion/backends, ontology, embeddings, LanceDB 스키마) | 비파괴 전환 정책으로 보존 | 다음 릴리스에서 제거 or `legacy/` 격리 + 문서화 |
| M-08 | P2 | 우측 '작업 진행' 헤딩이 "진행 중 1개"인데 목록 첫 항목이 '실패' 잡 — 카운트 의미 혼란 가능 | 진행 카운트 산정과 표시 목록 필터 불일치 추정 (검증 중 스크린샷에서 관찰) | 카운트 기준 명확화(진행=running/queued만) + 실패는 별도 배지 |
| M-09 | P2 | 문서작성 구조 편집 후 '미리보기' 탭이 빌드 전까지 갱신되지 않음(서버 preview 의존) | 미리보기는 structure 응답/build 응답의 preview 필드만 표시 | 클라이언트 측 렌더(구조→개조식 평문)로 편집 즉시 미리보기, 빌드 시 서버 값으로 확정 |
| M-10 | P3 | 채팅 응답과 컴포저 사이 과대 여백(스크린샷 기준), 메시지 폭 최대치 미제한으로 장문 라인 길이 김 | 채팅 스크롤 영역 flex 배치 + 메시지 max-width 부재 | 대화 영역 하단 정렬·메시지 max-width(~72ch) 검토 |
| M-11 | P2 | 문서작성 출력 유형 칩의 **미선택 상태 라벨이 거의 안 보임**(시행문/풀버전/이메일 — 회색 위 저대비) | `.icon-button--labeled` 비활성 상태 라벨 색이 배경 대비 부족 (스크린샷 확인) | 비활성 라벨 색을 본문 잉크(#111214) 유지, 배경만 옅게 — WCAG 4.5:1 확보 |
| M-12 | P2 | 문서작성 세션 요약 힌트박스 레이아웃 깨짐 — 제목·일정명·"대화 4개"·안내문이 3열로 단어 중간 줄바꿈("주간보 고 준비") + 세션명/일정명 중복 표기 | 힌트박스 내부가 grid 3열로 좁게 분할되어 한국어 어절 중간 개행 | 1열 세로 배치(세션명 → 메타 배지(대화 N·일정명) → 안내문), `word-break: keep-all` 적용 |

## AI 최종 리뷰 발견 (2026-07-04, 미검토 영역 3관점 정밀 스윕 — 우측 패널 C / 전역 정합성 G / 사용자 여정 J)

### C. 우측 컨텍스트 패널 + 승인/작업 흐름 (14건)

| # | 우선순위 | 항목 | 코드 근거 | 개선 방향 |
| --- | --- | --- | --- | --- |
| C-01 | **P1** | '가까운 일정'이 미래가 아닌 **과거 일정 5건**을 표시 (기능 의미 반전 버그) | `app.tsx:8005-8007` 현재시각 필터 없이 오름차순 slice / 서버도 전체 반환 `app.py:432` | `starts_at >= now` 필터 + 빈 상태. D-06 '오늘 일정'과 유틸 공유 |
| C-02 | **P1** | 승인 카드가 `documents.finalize`·`document_output`·`pending` 원시 키 노출 — **무엇을 승인하는지 알 수 없음** | `app.tsx:8289-8292`, 티켓 스키마에 대상 상세 없음 `api.ts:572-581` | 액션 한국어 라벨 맵 + 서버 `target_label`(문서 제목 등) 추가 + describeStatus 적용 |
| C-03 | **P1** | 작업 카드에 내부 락 키 `resource_key`(`work_session:uuid…`) 원문 노출 | `app.tsx:8345` | 표시 제거 or 한국어 변환("업무대화: 세션명"), 잡 id는 상세로 |
| C-04 | **P1** | 메뉴별 상세 렌더러 `renderActiveDetailPanel` ~150줄이 **미호출 죽은 코드** — 선택 상태 카드는 제목 2줄뿐 | 정의 `app.tsx:7857-8002`, 호출 0건, 실카드 `:8236-8245` | 의도 배선(카드 본문화+클릭 이동) or 삭제 후 카드 재설계 |
| C-05 | P2 | 승인 후 후속 안내 단절 (승인≠실행 2단계인데 다음 단계 미안내) | `app.tsx:3752-3762`, 안내는 문서작성 화면에만 `:6314-6319` | 승인 토스트에 [최종 저장 적용으로 이동] CTA / 승인+적용 통합 옵션 |
| C-06 | P2 | 응답 맥락 상세가 세션 전환 후 잔존 — 제목/수치가 다른 세션 혼합 | 상태 `app.tsx:1391` 리셋 0건, 렌더 `:8246-8264` | 세션/메뉴 전환 시 리셋, 구조 데이터 참조로 |
| C-07 | P2 | "GraphRAG 근거 N개"/"GraphRAG 인덱싱 실행" 구용어 노출 + 동일 화면 "지식위키"와 혼용 | `chatContextSummary.ts:9-11`, `executionLogDisplay.ts:9` | "지식위키"로 표시 문자열 즉시 교체 |
| C-08 | P2 | 작업 카운트(50건 집계) vs 목록(8건 렌더) 모수 불일치 — 안 보이는 활성 잡은 취소 불가. cancel_requested에도 취소 버튼 중복 노출 | `app.tsx:8014-8015`, `:8385-8390` | 활성 잡 상단 고정 정렬 후 slice, 취소 중 상태 비활성 표기 |
| C-09 | P2 | 패널 강제 자동 열림이 사용자 닫기 선택을 반복 무시 (채팅 응답·일정 등록·엔진 제어 등 트리거 과다) | `app.tsx:2366-2376` + 호출부 9곳 | 자동 열림은 승인·실패 한정, 나머지는 탭 아이콘 배지 |
| C-10 | P2 | 작업/최근실행 탭이 동일 아이콘(panel-logs.png), aria-pressed 부재, 비활성 사유 미안내 | `app.tsx:8024-8025`, `:8207-8225` | 전용 아이콘 + aria-pressed + 사유 툴팁 + 수량 배지 |
| C-11 | P2→P1(D-04 시) | D-04/D-05 실행 시 고아 5종: 미리보기 탭(search 조건) `:8027,:8451-8489` / ContextPanelKey "preview" `:169` / 승인 빈상태 "외부 실행, 파일 변경" 문구 `:8281` / 죽은 search 분기 `:7958-7983` / executionLogDisplay 구 라벨(유지 권장) | 좌동 | D-04 구현 PR 체크리스트 등재 |
| C-12 | P2 | 덤프 뷰어: 폴백이 오래된 잡 선택 가능·실행 일시 미표시·절대경로/원시 필드/개발자 용어("덤프","풀로그") 노출 | `app.tsx:8009-8013`, `:8502`, `:8529`, `:8536-8539` | "색인 상세 로그"로 순화, 최근 완료/실패 잡 기준, 경로는 [폴더 열기]로 |
| C-13 | P3 | 작업 로그가 최초 1회 캐시 후 미갱신 (진행 중에도 고정) | `app.tsx:2415-2433` | 활성 잡은 폴링에 증분 재조회 포함 |
| C-14 | P3 | 패널 폭/열림/섹션 상태 미영속(재시작 초기화) + 리사이저 키보드 접근 불가 | `app.tsx:1434-1453`, `:8194-8200` | localStorage 영속 + 키보드 조절(aria-valuenow) |

### G. 전역 정합성 (17건)

| # | 우선순위 | 항목 | 코드 근거 | 개선 방향 |
| --- | --- | --- | --- | --- |
| G-01 | **P1** | 엔진 다운 시 **에러 토스트 무한 스팸**(1.5초마다 신규, 상시 4~5장) + 원시 예외 노출 | 폴링 `app.tsx:2072`→catch `1940`→토스트 `1774-1780`, dedupe 없음 `1566-1578` | silent 폴링 실패는 배너 1회, pushToast dedupe, 한국어 고정 카피 |
| G-02 | **P1** | 새로고침 1클릭에 유사 문구 info 토스트 2장 동시 발송 | `app.tsx:8101-8102` (setNotice+pushToast 연속 호출) | 1종으로 통일 |
| G-03 | P3 | 토스트 톤 info/error 2종뿐(성공 톤 부재) + 닫기만 lucide X(자산 체계 이탈) | `app.tsx:191-195`, `:8581` | success 톤 추가, close.svg 통일 |
| G-04 | **P1** | 컴포저 "파일 첨부"가 label — **키보드 포커스/실행 불가** | `app.tsx:4959-4969` | button+ref.click()으로 교체 (F-04와 동일 지점) |
| G-05 | P2 | 같은 "파일 열기" 액션에 화면마다 folder-open/file 아이콘 혼용 | `app.tsx:7131,7200` vs `5216,6332` | 파일=file, 위치=folder-open 전 화면 통일 |
| G-06 | P2 | 구·신 버튼 체계 공존: 톱바 .topbar-icon-button(+텍스트 글리프 +/-), dead CSS(.chat-composer__plus-button), 색인 CTA 아이콘 미적용 | `app.tsx:8077-8125`, `styles.css:1253-1267`, `app.tsx:6814-6885` | 톱바 icon-button 마이그레이션 완료, dead CSS 제거, 매핑표 적용 |
| G-07 | P2 | disabled 사유 툴팁 제공이 화면마다 상이 (스캔/색인/최종적용 등 미제공) | 모범 `app.tsx:6819-6823` vs `6866,6874,6882`, `6305-6312` | disabled 시 title 사유를 전역 규칙화 |
| G-08 | P3 | 빈 상태 EmptyState/subtle-text 혼용, 로딩 접미("중…"/"중입니다...") 혼재 | `app.tsx:7235,7256,7279,6150,8369,8521`, `7316` vs `8189` | 목록형은 EmptyState, 로딩 접미 1종 |
| G-09 | **P1** | "색인"↔"인덱싱" 혼용 (같은 문장 안 혼용 포함, 사이드카 stage 문자열이 UI 직결) | `app.tsx:8101,6727,8563,7897,7912,8502`, `app.py:2485,2508,2999,3011`, `knowledge_wiki.py:207,266,344,1122` | 사용자 노출 "색인" 전면 통일(서버 포함) |
| G-10 | **P1** | Content Base/콘텐츠 베이스/작성 컨텐츠 3중 표기('컨텐츠'는 비표준 표기) | `app.tsx:201,277,857,1671,3517,6263,6280,7936`, `executionLogDisplay.ts:4` | "작성 콘텐츠" 1종 통일, 영어 제거 |
| G-11 | **P1** | **최종 HWPX 산출물 본문에 "Content Base" 내부 용어 삽입** (결재 문서에 노출) | `hwpx_writer.py:132,521,665`, `documents.py:507` | 산출물 폴백 문구를 업무 문서 톤으로 교체 — 최우선 |
| G-12 | P2 | 지식위키/지식 위키/지식폴더 위키/지식베이스 4중 표기 + "엔진" 3종(업무/지식/검색) 혼재 | `app.tsx:1226,2352,3870,7227…` vs `1596,1892,…`, `2845,6639`, `1223,6442` | "지식위키" 1종 확정 치환, 엔진은 업무 엔진만 |
| G-13 | P2 | 세션 계열 4종(대화세션/대화 세션/업무대화 세션/업무 세션) + "직접 작성" vs "바로 작성" | `app.tsx:4108,5997,3195,4064…`, `6008` vs `7940` | "업무대화 세션"(축약 "세션") + "직접 작성" 통일 |
| G-14 | P3 | 나브 "기타 환경설정" vs 전역 "환경설정" | `app.tsx:205` vs `6467,6821` | D-05에서 "환경설정" 확정 |
| G-15 | P2 | 원시 내부 값 노출 묶음: `external_app_preferred` 문장 삽입 `7753` / 프로필 카드 providerKey `2669` / "runner 0개" `8144` / "트라이그램" `7103` / "score 12" `5182` / 채팅 provider/model 원시 id `4746-4748` / 설정 그리드 영어 라벨·raw 값 `7763-7814` | 좌동 | 한글 라벨 매핑 원칙화(describeStatus 패턴) |
| G-16 | P2 | 영어 eyebrow 3건(calendar-first planner / Anything history / local-first settings) | `app.tsx:4412,5313,7581` | 한글 통일 (5313은 D-04로 소멸) |
| G-17 | P2 | 사이드카 '사용법 안내' 하드코딩이 확정 메뉴 체계와 불일치("2. 파일찾기", "Content Base") | `app.py:1142-1162` | D-04/D-05 반영 시 갱신 체크리스트 등재 |

### J. 사용자 여정 완성도 (14건)

| # | 우선순위 | 항목 | 코드 근거 | 개선 방향 |
| --- | --- | --- | --- | --- |
| J-01 | **P1** | 업무대화 첫 실행이 CTA 없는 막다른 화면 (+버튼 위치 안내도 부정확) | `app.tsx:5060-5065`, `4114-4122`, `4060-4072` | 빈 상태에 [새 세션 만들기] / 세션 없이 컴포저 노출→첫 전송 시 자동 생성 |
| J-02 | **P1** | LLM 미설정 사전 안내 전무 — 첫 실패로만 인지, 환경설정 이동 버튼 없음 | `app.tsx:4983-4992`, `6076-6086`, `6460-6468` | 공통 판정 유틸 + 컴포저/문서작성/지식폴더 배너 [환경설정으로 이동] |
| J-03 | **P1** | 채팅 실패 시 입력·첨부 소실, 제자리 재시도 불가 | `app.tsx:2902-2907`(선비움), `3042-3061`(복원 없음) | 실패 시 draft/첨부 복원 + [다시 시도] |
| J-04 | **P1** | 구조 재생성 실패/중단 시 기존 편집 내용 소실 (롤백 없음) | `app.tsx:3400-3410`, `3430-3449`, `3458-3460` | 요청 전 스냅샷 → 실패/중단 시 복원 |
| J-05 | P2 | 문서작성 세션 기반 모드, 세션 0개면 해결 경로 없는 데드엔드 | `app.tsx:6013-6038`, `3380-3383` | 인라인 안내 + [업무대화에서 세션 만들기] CTA |
| J-06 | P1(설계) | 첫 실행 최악 3화면 = 업무대화·위키 탭·문서작성 세션 모드 → D-06 온보딩 카드 1순위 입력 | `5060-5065`, `7317-7322`, `6013-6038` | 홈 브리핑 온보딩 시나리오(①LLM ②지식폴더 ③첫 세션) |
| J-07 | P2 | 위키 빈 상태·검색 0건이 원인 구분 없는 텍스트뿐 (색인 0개 vs 키워드 불일치) | `app.tsx:7317-7322`, `7109-7113`, `7067-7080` | 인라인 [색인 처리로] CTA + 원인 분기 카피 |
| J-08 | P2 | 지식 검색/근거답변 결과에서 다음 액션 단절 (위키 카드 보기·세션 연결·대화 이어가기 부재) | `app.tsx:7123-7142`, `7161-7221` | 결과 카드 액션 확장 + [업무대화로 이어가기] (F-14 스펙에 포함) |
| J-09 | P2 | 위키 문서 카드→채팅 질문/관련 검색 핸드오프 부재 | `app.tsx:7324-7345` | F-18 헤더에 [이 문서로 질문하기]/[관련 검색] |
| J-10 | P2 | 실패한 색인 잡 카드에서 제자리 재시도 불가 (재실행 버튼이 queued/partial 한정) | `app.tsx:7034-7044` | failed에 [다시 색인] + 실패 파일만 재시도 검토 |
| J-11 | P2 | 엔진 미가동 시 전 화면이 '조용한 빈 상태' — 원래 빈 것과 구분 불가, 복구 버튼은 팝오버 속 | `app.tsx:1873-1874,1939-1940`, `8126-8180` | 지속 배너 "엔진 미연결" + [시작]/[재연결] CTA (G-01과 세트) |
| J-12 | P3 | 연결 파일 팝오버 Escape/바깥클릭 닫기 없음 + 9개째부터 표시 불가(slice 8) / 문서작성 지시 Ctrl+Enter 미지원 | `app.tsx:4858-4884` vs 모범 `1803-1852`, `6041-6059` | 공통 팝오버 닫기 패턴 + "외 N개" + Ctrl+Enter |
| J-13 | P3 | [LLM 연결 테스트]가 예고 없이 설정을 먼저 저장 (실패해도 유지, 복구 불가) | `app.tsx:2706-2727`, `7742-7751` | dry-run 테스트 경로 or "저장 후 테스트" 정직 표기+복원 (F-16과 함께) |
| J-14 | P3→P1(D-04 시) | 삭제 예정 기능 참조가 유지 화면에 잔존: 실행기록 빈상태 "파일정리" `7528-7531` / 설정 anything 문구 `7752-7754` / **채팅 [파일 연결]→삭제될 화면 이동** `2816-2823` / 작업 빈상태 문구 `8322` | 좌동 | D-04 체크리스트 등재 (③은 파일 연결 모달로 대체 필수) |

## 처리 이력

- (라운드 기록은 2026-07-04-core5-integration-test-plan.md 참조 — I1(검색 범위 카운트 0)·I3(위키 트리 중복 키)·설정 폼 폴링 덮어쓰기는 검증 중 즉시 수정 완료)
