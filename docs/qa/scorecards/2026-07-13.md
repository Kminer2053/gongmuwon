# 1호 스코어카드 — 2026-07-13

대상: v0.1.3 설치본(`Gongmu_AI_Ollama_Gemma4_E2B_IT_Multimodal_20260713-1419`), 실폴더 `2026_AI혁신처`(426건) 전량 색인+보강 완료 상태.
기준: `docs/qa/release-acceptance-plan.md`. 반복 규칙(§0)은 시간 제약상 대부분 **1회 실행**으로 축소 — 아래 각 항목에 실제 반복 횟수를 명시.

## 종합 점수: **81.25 / 100** (출시 기준 90 미달 — CONDITIONAL)

| 게이트 | 판정 | 점수 |
|---|---|---|
| G1 설치 | PASS | 1.0 |
| G2 첫실행 | PASS | 1.0 |
| G3 색인 | PASS | 1.0 |
| G4 검색신뢰 | CONDITIONAL | 0.5 |
| G5 대화연계 | **FAIL** | 0.0 |
| G6 위키품질 | PASS | 1.0 |
| G7 문서품질 | PASS | 1.0 |
| G8 회귀 | PASS | 1.0 |
| **합계** | | **6.5/8 → 81.25** |

전날 대비: 최초 판정(G1/G2/G3/G6, 7/12~13)은 그대로 PASS 유지. 오늘 신규 실행한 G4/G5/G7/G8에서 **G5가 이번 회차 신규 결함으로 FAIL** — 4대 문제의식 중 1번("업무대화창 중심 연계흐름")이 실측으로 재확인됨.

---

## G8 회귀 (전체, 1회)
- 사이드카 pytest: **427 passed**
- 데스크톱 vitest: **234 passed** (35 files)
- PASS

## G4 검색신뢰 (1회, 반복 부족 — 다음 회차 3회 필요)
- **키워드 8/8 PASS**: 안전관리등급·승진 전보·업무계획·회의운영·인사·감사·사업계획서·계약 구매 — 전부 hitCount=20(상한)
- **자연어 8문 ask: 4/8 명확 PASS**, 4/8 "정보를 찾을 수 없음" 응답
  - PASS: 안전관리등급 매김 기준(공공기관 안전관리등급제 지침 인용), 업무계획(AX경영비전·로드맵 인용), 감사 지적사항 처리(감사규정·안전점검지침 인용), 계약·구매(로컬화 사업 조달자료 인용)
  - "찾을 수 없음": 승진·전보 기준, 회의운영 규정, 법무·규정 전반, 민원처리 절차
  - **원인 진단**: retrieve() 직접 조회로 교차검증한 결과, 위 4개 질의는 raw 검색 단계부터 관련성 낮은 문서만 반환됨 — 회의운영·민원처리 주제 태그가 달린 문서 36/33건을 열어보니 실제로는 ISO45001 절차서·이사회운영규정 등 **광범위 태깅된 문서**이지 "회의운영 규정"·"민원처리 절차"를 직접 다루는 문서가 아니었음. **AI혁신처 폴더 자체가 AI전략 중심 기관이라 일반행정(민원·회의운영) 1차 자료가 얕음** — 시스템이 창작하지 않고 정직하게 "없음"이라 답한 것이므로 반드시 버그는 아니나, **질의셋 설계가 이 기관 데이터 실태와 안 맞음**. 다음 회차부터 벤치 질의를 실제 1차 자료가 있는 주제(인사규정 구성, 이사회운영규정 핵심 등)로 교체 필요.
- **판정: CONDITIONAL** — 키워드 검색은 신뢰도 높음. 자연어 벤치는 재설계 후 재실행 요망.

## G5 대화연계 — **FAIL** (핵심 신규 발견, 1회씩만 실행)
| TC | 결과 |
|---|---|
| L-01 지식질의→보고서 | **FAIL**. 1턴: "안전관리등급 기준?" 질의 → 정직한 "근거 없음" 답변(citations 2). 2턴: "방금 내용을 1페이지 보고서로" → 문서 생성은 성공(54초)했으나 **제목이 "AX포털 개선 계획 요약 보고"로, 직전 대화 주제(안전관리등급)와 전혀 무관**. 직전 맥락이 문서에 반영되지 않음 |
| L-02 멀티인텐트(일정+이메일) | **FAIL(심각)**. "다음주 화요일 3시 회의 일정 잡고 그 내용으로 이메일도 써줘" → 실행된 것은 **`help.guide`(일반 사용법 안내)** 뿐. 일정 미등록(`schedulesAfter: []`), 이메일 미생성. 의도 라우터가 복합 지시를 완전히 놓치고 도움말로 폴백함 |
| L-03/04/05 | 시간 제약으로 이번 회차 미실행 — 다음 회차 우선 실행 |
| L-06 컨텍스트 초기화 | PASS. 초기화 후 무관 질의에 이전 주제(감사) 미혼입 확인. (참고: 초기화 직전 턴이 featherless API 504로 실패 — 외부 서비스 일시 장애, 앱 결함 아님. 단 해당 턴 소요 60.3초로 G5 시간기준(≤60초) 근소 초과 — 재시도 정책 점검 필요) |
- **판정: FAIL** — L-01/L-02 모두 결함. 특히 L-02는 핵심 기능(복합지시 인식) 완전 미작동으로 **최우선 수정 대상**.

## F-01 지식폴더 등록→색인→분류확정→보강
- 색인: 426/426, 실패 0 (G3 재확인)
- 업무영역: **25개**(≥10 기준 PASS), 이름 마커·연도 제거 확인(상위 16개는 깨끗). **단, 9개 항목이 "AI/도입/전략/수립/직원/데이터/기반/과제/예산" 같은 단어조각으로, 실제 업무영역이 아닌 노이즈** — F-2x 백로그
- **신규 중대 발견**: 업무(허브) 분류 실제 문서 배정 — **414건 중 3건만 high-confidence 배정**(청년창업 관련 2, 정부경평 피드백 1), **296건 low-confidence "분류 대기"**, **127건 미매칭**. 대시보드 "지금 적용" 재실행해도 변화 없음(원인: 신뢰도 임계값 미달로 자동 배정 자체가 설계상 보류됨). **사실상 업무 허브가 전체 문서의 0.7%만 커버** — 4대 문제의식 #2("분류체계가 폴더를 실제로 엮는지")가 데이터로 재확인됨. **최우선 백로그**
- 판정: 색인/영역생성은 PASS, **문서-허브 배정은 FAIL** (별도 이슈로 분리 기록)

## F-02/F-03 검색·근거답변 — G4에 통합 기록 (위 참조)

## F-04 일정
- API: 등록/조회/이번주 필터/알림 due/삭제 전부 정상
- UI: 월/주/일 뷰 전환 정상 렌더, 알림배너("곧 시작: … N분 후") 정상 표시
- **경미한 버그 발견**: 자정을 넘나드는 시간외(종일·시간외 행) 일정이 인접 두 날짜 칸에 중복 렌더링됨(실제 DB 행은 1건). 근본 원인: `buildTimegridDay`가 클리핑된 종료시각이 아닌 원본 시작시각으로 라벨을 both-day에 push. 실사용 빈도 낮은 엣지케이스 — 백로그 등록, 비차단
- 판정: PASS(경미 결함 1건 별도 기록)

## F-05 문서작성 4양식
- officialMemo/onePageReport/fullReport/email 전부 structure+build 성공(4/4, HTTP 201)
- placeholder 0건(자동검사 1건 오탐 — "[안내]" 같은 정상 공문 관용구를 오검출, 실제 결함 아님)
- 수치 보존: 날짜·금액·인원 등 의미값은 100% 보존되나 **표기 형식이 자연스럽게 재구성됨**(예: "8월 1일부터 8월 31일까지"→"2026년 8월 1일 ~ 2026년 8월 31일"). 문자열 완전일치 기준이면 낮게 나오나 의미보존 기준으로는 정상 — 다음 회차부터 수치 추출 비교로 검사방식 개선 필요
- 판정: PASS

## F-06 위키 UI
- 문서 미리보기 모달: 개요·LLM요약·섹션아웃라인·키워드·주제·원본경로 정상 렌더 — PASS
- 위키 상세 "← 이전"·"← 목차" 버튼 모두 존재·동작 — PASS
- 버전군(사본) 배지 확인("사본 2개") — PASS

## F-07 어휘집
- 기관팩 정상 임포트: 이전 회차 확인(내장 65+기관 10)
- **충돌 반려 재확인**: 동의어 충돌 팩 제출 → `ok:false` + 정확한 충돌 사유 메시지 반환 — PASS
- 주제 병합·삭제: 실사용 데이터 훼손 방지를 위해 이번 회차 실제 병합은 미실행. `test_wiki_topic_admin.py`가 G8 회귀에 포함되어 PASS 확인(간접)

## F-08 실행기록·환경설정
- 실행기록: 정상 기록 확인(work_session.turn.failed 항목에서 외부 API 504 원문까지 정확히 로깅됨 — 오히려 진단성 우수)
- 프로필 전환: **보안 제약(설정파일/API키 노출 금지)으로 이번 회차 미실행** — UI 클릭 기반 비파괴적 검증만 다음 회차 권장

## W-01~03 위키 답변 사용 검증 (고정 질의 8문, 1회)
- W-01/W-02 (정합성+출처): **8/8 PASS** — 전 질의 citations≥4, 위키 종합 내용과 사실 일치(예: "공공안전지수"=PSI 정의 정확 인용, "AI 업무 추진 계획"=조직개편·로드맵 구체 인용)
- W-03 (근거없음 정직성): 답변 텍스트는 정직("찾을 수 없습니다") — **PASS**. 단 **"찾을 수 없다"면서도 무관 문서 5건을 citations로 계속 첨부**하는 UX 결함 발견 — 백로그(근거없음 판정 시 citations 억제 필요)

---

## 신규 백로그 (우선순위순)

1. **[최우선] L-02 복합지시(멀티인텐트) 완전 미작동** — "일정 등록 + 이메일 작성" 같은 복합 지시가 의도 라우터에서 `help.guide`로 폴백, 두 액션 모두 미실행. 실사용 빈도가 높을 패턴이라 즉시 조사 필요
2. **[최우선] 업무(허브) 문서 배정 0.7%** — 296건 low-confidence 대기, 127건 미매칭. `match_work_area` 신뢰도 산정 로직이 실제 중첩 폴더 구조에서 거의 항상 low로 떨어짐. 재적용으로 해결 안 됨(설계상 자동승격 없음) — 신뢰도 임계값 또는 폴더 매칭 알고리즘 재검토, 혹은 296건 review 큐에 vocab 후보처럼 자동추천(auto-triage) 도입 검토
3. **[높음] L-01 문서작성 연속성 단절** — 직전 대화 주제가 문서 생성에 반영 안 되고 다른 주제로 생성됨. document.create 스킬이 세션 컨텍스트를 어떻게 선택하는지 조사 필요
4. **[중간] G4 벤치 질의셋 재설계** — 이 기관 실데이터에 맞는 질의로 4문 교체 (승진전보/회의운영/법무규정/민원처리 → 실제 1차자료 있는 주제로)
5. **[낮음] W-03 무관 citations 미억제** — "근거없음" 답변 시 citations 빈 배열로
6. **[낮음] F-01 노이즈 업무영역 9개** — 단어조각("AI","도입","전략" 등) 필터링
7. **[낮음] F-04 자정교차 일정 중복렌더링** — buildTimegridDay 라벨 로직 수정
8. **[정보] featherless API 504 1건** — 외부 서비스 일시 장애, 앱 결함 아님. 재시도/타임아웃 정책 점검 권고

## 미실행 항목 (다음 회차 우선)
- L-03(근거답변→업무대화 이어가기) / L-04(검색결과→세션연결) / L-05(대화 문서작성→이어작성)
- G4/G5/W 전체 3회 반복(오늘은 시간 제약으로 1회)
- F-08 프로필 전환(비파괴적 UI 검증)

## 재사용 노트
- 이번 회차에서 확정한 G4 키워드 8문·W 8문+무응답 1문은 **다음 회차부터 고정 질의셋으로 그대로 재사용**(계획 §0 요건 충족)
- G4 자연어 8문은 위 사유로 **폐기, 재설계 필요** — 차기 스코어카드 작성 시 새 질의셋 확정 후 고정 → 2026-07-14 신규 질의셋 확정(release-acceptance-plan.md G4 표 참조)
